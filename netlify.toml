[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Scheduled Functions live in netlify/functions/ and declare their own
# `schedule`. drip-cron runs every minute to dispatch due drip messages.
