# ─── LINE Messaging API (LINE Developers Console) ─────────
LINE_CHANNEL_ACCESS_TOKEN=
LINE_CHANNEL_SECRET=

# ─── LIFF (survey app) ────────────────────────────────────
LIFF_CHANNEL_ID=
NEXT_PUBLIC_LIFF_ID=

# ─── Database (Neon Postgres) — added at Step 4 ───────────
# DATABASE_URL = Neon POOLED string (host contains "-pooler"). Used at runtime.
DATABASE_URL=
# DIRECT_URL = Neon DIRECT string (same host WITHOUT "-pooler"). Used ONLY by
# `prisma db push` / migrations. Required so tables get created correctly.
DIRECT_URL=

# ─── Funnel assets (added at deploy) ──────────────────────
NEXT_PUBLIC_MAIN_VIDEO_URL=
NEXT_PUBLIC_IFA_VIDEO_URL=
NEXT_PUBLIC_SCHOOL_VIDEO_URL=
IFA_BOOKING_URL=https://tayori.com/f/wpyuka/
SCHOOL_LINK_URL=https://line.leadmail.info/s/0erE2l4o
IFA_SITE_URL=https://wealth-partner-re.com/
SCHOOL_SITE_URL=https://www.gapartners.co.jp/mtu/

# ─── Cron auth (added at Step 5) ──────────────────────────
CRON_SECRET=
# DRIP_TEST_MODE=1 schedules steps seconds apart for fast local testing
DRIP_TEST_MODE=
